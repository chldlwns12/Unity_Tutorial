using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test9 : MonoBehaviour
{

    ParticleSystem ps;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //시작
        //ps.Play();
        
        //정지
        //ps.Stop();

        //일시정지
        //ps.Pause();

        //버스트 같이 한번에 몇개의 파티클을 재샐할지
        //ps.Emit(100);
    }
}
